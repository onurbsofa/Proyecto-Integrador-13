<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles-log-y-reg.css">
    <link rel="stylesheet" href="/css/styles-header.css">
    <link rel="stylesheet" href="/css/styles-footer.css">
    <link href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://kit.fontawesome.com/13e38d5bb6.js" crossorigin="anonymous"></script>
    <link rel="shortcut icon" href="/img/favicon.ico">
    <title>Tostao</title>
</head>
<body>
    
    <!--Include Header-->
    <%- include('../partials/header') %>

    <main>
        <div class="color_especial header">      
            <!--falta indicador pagina-->           
            <h2>Registrate</h2>
            <h3>Por favor, completá los siguientes datos:</h3> 
        </div>


        
        <form action="registro" method="POST" enctype="multipart/form-data" id="formulario" class="container1" >   
            <div>   
                <div>
                    <!--nombre del usuario-->
                    <div class="celda">
                        <label for="nombre">Nombre Completo<span class="asterisco">*</span></label>
                        <div>  
                            <input type="text" id="nombre" name="name" value="<%= locals.old && old.name %>" class="form-control input" required> 
                            <span></span>  
                            <!-- si hubo errores en este campo del form, devuelve un mensaje de error -->
                            <% if (locals.errors && errors.name) { %>
                                <p class="feedback"><%= errors.name.msg %></p>
                            <% } %>      
                                                               
                        </div>
                        
                    </div>

                    <!--email del usuario-->
                    <div class="celda" id="campos_email">             
                        <label for="email" >Email<span class="asterisco">*</span></label>
                        <div class="input-group flex-nowrap">
                        </div>
                        <input type="email" id="email" name="email" class="form-control input" value="<%= locals.old && old.email %>" id="email" required>
                        <span></span>
                        <!-- si hubo errores en este campo del form, devuelve un mensaje de error -->
                            <% if (locals.errors && errors.email) { %>
                                <p class="feedback"><%= errors.email.msg %></p>
                            <% } %>  
                            
                    </div>

                    <!--imagen de perfil del usuario-->
                    <div class="celda" id="campos_imagen">       
                        <label for="formFile"  class="form-label">Selecciona tu imagen de perfil<span class="asterisco">*</span></label>
                        <div>
                            <input type="file" id="imagen" class="form-control input"  name="imagen" required>
                            <span></span>
                        </div>
                    </div>

                              

                    <!--password del usuario-->
                    <div class="celda" id="campos_contraseña">       <!-- FALTA AGREGAR OPCION VER CONTRASEÑA -->
                        <label for="contraseña">Contraseña<span class="asterisco">*</span></label>
                        <div>
                            <input type="password" id="contraseña" class="form-control input" name="password" placeholder="Ej: Contraseña123" required>
                            <span></span>
                            <!-- si hubo errores en este campo del form, devuelve un mensaje de error -->
                            <% if (locals.errors && errors.password) { %>
                                <p class="feedback"><%= errors.password.msg %></p>
                            <% } %>  
                        </div>
                    </div>

                    
                    <!--confirmar password-->
                    <div class="celda" id="campos_contraseña2">      
                        <label for="confirmar_contraseña">Confirmá contraseña<span class="asterisco">*</span></label>
                        <div>
                            <input type="password" id="confirmar_contraseña" class="form-control input" name="confirmPassword" required>
                            <span></span>
                            <!-- si hubo errores en este campo del form, devuelve un mensaje de error -->
                            <% if (locals.errors && errors.confirmPassword) { %>
                                <p class="feedback"><%= errors.confirmPassword.msg %></p>
                            <% } %>  
                        </div>
                    </div>
                </div>
                
                <!--boton + captcha + redireccion-->
                <div>
                    <!-- falta captcha -->  
                    <button = type="submit" class="boton color_especial">Registrarte</button>                                
                    <p>¿Ya tenes una cuenta? <a href="/user/login" class="color_especial redireccion">Inicia sesión</a><p> 
                </div>
            </div>
        </form>

    </main>
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script> 
    <script src="/js/validacionRegistro.js"></script> 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>  
</body>
</html>